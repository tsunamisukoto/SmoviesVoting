import { SuggestionListComponent } from './suggestion-list.component';

describe('SuggestionListComponent', () => {
  let component: SuggestionListComponent;

  beforeEach(() => {
    component = new SuggestionListComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});

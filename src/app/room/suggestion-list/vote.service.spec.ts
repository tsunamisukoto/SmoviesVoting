import { VoteService } from './vote.directive';

describe('Service: Vote', () => {
  it('should create an instance', () => {
    const service = new VoteService();
    expect(service).toBeTruthy();
  });
});

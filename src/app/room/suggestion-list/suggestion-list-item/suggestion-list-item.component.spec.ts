import { SuggeestionListItemComponent } from './suggestion-list-item.component';

describe('SuggeestionListItemComponent', () => {
  let component: SuggeestionListItemComponent;

  beforeEach(() => {
    component = new SuggeestionListItemComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});

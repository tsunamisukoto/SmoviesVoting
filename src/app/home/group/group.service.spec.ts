import { GroupService } from './group.directive';

describe('Service: Group', () => {
  it('should create an instance', () => {
    const service = new GroupService();
    expect(service).toBeTruthy();
  });
});

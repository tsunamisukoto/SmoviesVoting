import { GroupListComponent } from './group-list.component';

describe('GroupListComponent', () => {
  let component: GroupListComponent;

  beforeEach(() => {
    component = new GroupListComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});

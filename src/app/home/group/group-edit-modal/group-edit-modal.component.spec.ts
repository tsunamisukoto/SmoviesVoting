import { GroupEditModalComponent } from './group-edit-modal.component';

describe('GroupEditModalComponent', () => {
  let component: GroupEditModalComponent;

  beforeEach(() => {
    component = new GroupEditModalComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});

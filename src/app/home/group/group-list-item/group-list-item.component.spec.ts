import { GroupListItemComponent } from './group-list-item.component';

describe('GroupListItemComponent', () => {
  let component: GroupListItemComponent;

  beforeEach(() => {
    component = new GroupListItemComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});

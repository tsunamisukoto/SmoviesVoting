import { RoomEditModalComponent } from './room-edit-modal.component';

describe('RoomEditModalComponent', () => {
  let component: RoomEditModalComponent;

  beforeEach(() => {
    component = new RoomEditModalComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});

import { RoomService } from './room.directive';

describe('Service: Room', () => {
  it('should create an instance', () => {
    const service = new RoomService();
    expect(service).toBeTruthy();
  });
});

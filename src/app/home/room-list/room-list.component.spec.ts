import { RoomListComponent } from './room-list.component';

describe('RoomListComponent', () => {
  let component: RoomListComponent;

  beforeEach(() => {
    component = new RoomListComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});

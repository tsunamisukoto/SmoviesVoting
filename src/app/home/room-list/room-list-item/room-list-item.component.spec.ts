import { RoomListItemComponent } from './room-list-item.component';

describe('RoomListItemComponent', () => {
  let component: RoomListItemComponent;

  beforeEach(() => {
    component = new RoomListItemComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
